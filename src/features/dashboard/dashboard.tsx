import React from 'react';
import { View, Text } from 'react-native'

export default function Dashboard() {
  return (
    <View style={{ backgroundColor: 'red', flex: 1 }}>
      <Text>Dashboard called</Text>
    </View>
  );
}